<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainFragment">
    <fragment
        android:id="@+id/mainFragment"
        android:name="com.bovink.androidlearning.MainFragment"
        android:layout="@layout/frag_main"
        android:label="MainFragment" >
        <action
            android:id="@+id/action_mainFragment_to_wordDetailActivity"
            app:destination="@id/wordDetailActivity" />
    </fragment>
    <activity
        android:id="@+id/wordDetailActivity"
        android:name="com.bovink.androidlearning.WordDetailActivity"
        android:label="WordDetailActivity" />
</navigation>